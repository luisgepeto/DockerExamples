version: '3'

services:
  api: 
    build:
      context: ./DockerAPI
    ports:
      - "8080:5000"
    networks:
      - my-network
    container_name: api

  mongodb:
    image: mongo
    networks:
      - my-network
    container_name: mongo

networks:
  my-network:
    driver: bridge