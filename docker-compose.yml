version: '3'

services:
  api: 
    build:
      context: ./DockerAPI
    ports:
      - "8080:5000"
    networks:
      - my-network
    container_name: api

  mongodb:
    image: mongo:3.4
    networks:
      - my-network
    container_name: mongo    

networks:
  my-network:
    driver: bridge